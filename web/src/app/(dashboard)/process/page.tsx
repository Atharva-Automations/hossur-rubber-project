'use client';

import { Header, PageContainer } from '@/components/global';

export default function ProcessTrackingPage() {
  return (
    <PageContainer>
      <Header
        title="Process Tracking"
        description="Monitor and track all production processes in real-time"
        icon="🔄"
      />
    </PageContainer>
  );
}
